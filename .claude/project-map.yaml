# Which planning/tracking surfaces this repo maps to.
# Read by the global `triage` skill (and any skill that needs the repo↔project link).
# Linear lifecycle rules, states, and roles live in CLAUDE.md ("Linear task lifecycle").

linear:
  team: NEO                  # NeonWave — TODO: → AND after Linear team move
  project: Starbase          # id: 38e4d31c-021e-4825-9d21-fd0bdc42aa35

obsidian:
  project: Starbase          # folder under PLANNING_VAULT_ROOT

github:
  repo: andromeda-engineering/starbase
